[package]
name = "stealthy-usage"
version = "1.4.9"
edition = "2021"
license = "MIT"
description = "Stealthy Usage"
homepage = "https://github.com/Xilous/stealthy-usage"
# The updater derives its GitHub owner/repo from this field at compile time
# (see github_repo() in src/updater.rs, which parses CARGO_PKG_REPOSITORY).
# Left on upstream, the fork would check CodeZeno's releases, download their
# binary and overwrite itself on the next update check.
repository = "https://github.com/Xilous/stealthy-usage"

[package.metadata.winres]
CompanyName = "Xilous"
ProductName = "Stealthy Usage"
FileDescription = "Stealthy Usage"
OriginalFilename = "stealthy-usage.exe"
InternalName = "StealthyUsage"
Comments = "https://github.com/Xilous/stealthy-usage"
# Retained: the code is Code Zeno's, used under MIT.
LegalCopyright = "Copyright (C) 2026 Code Zeno Pty Ltd"

[dependencies]
ureq = { version = "2", default-features = false, features = ["native-tls", "json", "proxy-from-env"] }
native-tls = "0.2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
dirs = "6"

[dependencies.windows]
version = "0.58"
features = [
    "Win32_Foundation",
    "Win32_Globalization",
    "Win32_Graphics_Gdi",
    "Win32_System_LibraryLoader",
    "Win32_UI_Shell",
    "Win32_UI_WindowsAndMessaging",
    "Win32_UI_Accessibility",
    "Win32_System_Registry",
    "Win32_System_Threading",
    "Win32_Security",
    "Win32_UI_Input_KeyboardAndMouse",
    "Win32_UI_HiDpi",
]

[build-dependencies]
winres = "0.1"

[profile.release]
opt-level = "z"
lto = true
strip = true
codegen-units = 1
panic = "abort"
